rebuild-db builds a staging db: olist_stg
copy this database and save as olist
use olist

instructions:

1. use ssms import wizard to import csv file
    in ssms:
    right click olist
    tasks
    import flat file
    specify input file
        'location of file to be imported'
            path/to/data/external/interim/claud_seller_names_260228.csv
        'new table name'
            dim_seller_gen_names
        'table schema'
            sales
        next
    preview data
        use rich data type detection
        next
    modify columns
        column name, data type, primary key, 0 allow nulls
        seller_id, char(32), 1, 0
        seller_gen_name, nvarchar(50), 0, 0
        no range
        next
    summary
        finish

2. run all .sql files in queries/analysis/globals/
    functions.sql
    view.sql

3. run queries/dev/cleaning/product_category_name_translation.sql