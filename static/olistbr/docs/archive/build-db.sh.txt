##!/usr/bin/env bash

# scrapped
# dropping and rebuilding now done in python

# paths

# THIS_PATH=$(realpath "$0")
# PROJECT_PATH=$(dirname "$THIS_PATH")
# RAW_PATH="$PROJECT_PATH/data/raw"

# SERVER='localhost'
# DB_NAME='olist'
# DB_STG_NAME=$DB_NAME'_stg'

# get_raw_file_paths() {
#     # create array
#     # -a make indexed arrays
#     # -A make associative arrays
#     declare -a raw_file_paths

#     for raw_file_path in "$RAW_PATH"/*.csv; do
#         # echo $raw_file_path
#         # filename=$(basename "$raw_file_path")
#         raw_file_paths+=("$raw_file_path")
# 	    # echo "$raw_file_path"
#         # echo $filename
#     done
    
#     echo "${raw_file_paths[@]}"
# }

# raw_paths=$(get_raw_file_paths)
# echo $raw_paths

# # TODO: get output global so var can use outside function
# # https://www.linuxjournal.com/content/return-values-bash-functions

# # raw_file_paths=$(get_raw_file_paths)
# # echo "${raw_file_paths[*]}"
# # raw_file_paths="$(get_raw_file_paths)"
# # echo $raw_file_paths

# # sqlcmd -C -S $SERVER -E -Q 'select @@servername;'
# # sqlcmd -C -S $SERVER -E -i queries/build-db/000_recreate_db.sql -v DB_STG_NAME=$DB_STG_NAME
# # sqlcmd -C -S $SERVER -E -i queries/dev/0_test.sql
# # sqlcmd -C -S $SERVER -E -i queries/dev/1_test.sql

